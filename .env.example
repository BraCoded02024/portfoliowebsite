# Public site URL (no trailing slash). Used for canonical URL, Open Graph, and JSON-LD.
# Must match the domain you submit in Google Search Console and your sitemap.xml / robots.txt.
VITE_SITE_URL=https://bernardowusu.com
